const array = ["Jerald", "Praveen", "Devadass", "Derrick"];
const sorted = array.sort();
console.log(sorted);

// This won't work for numbers
const sortedNumbers = [6, 4, 15, 10].sort();
console.log(sortedNumbers);
